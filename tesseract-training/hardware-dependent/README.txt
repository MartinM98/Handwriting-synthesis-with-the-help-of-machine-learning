There are three directories containing scripts which install and train tesseract:

- apt-get : which uses apt package manager (dedicated for Linux OSes)
- Homebrew : which uses brew package manager (dedicated for MaxOS)
- pacman : which uses pacman package manager (dedicated for Linux OSes)
